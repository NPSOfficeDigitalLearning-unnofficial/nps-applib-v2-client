import React, { ReactNode } from "react";
import "./Admin.scss";

export default class AdminPage extends React.Component {
    render():ReactNode {
        return (
            <main>
                TODO: admin page
            </main>
        );
    }
}