import React, { ReactNode } from "react";
import "./About.scss";

export default class AboutPage extends React.Component {
    render():ReactNode {
        return (<>
            <main>TODO: about page</main>
        </>);
    }
}