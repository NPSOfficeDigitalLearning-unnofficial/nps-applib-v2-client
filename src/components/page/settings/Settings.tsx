import React, { ReactNode } from "react";
import "./Settings.scss";

export default class SettingsPage extends React.Component {
    render():ReactNode {
        return (<>
            <main>TODO: settings page</main>
        </>);
    }
}